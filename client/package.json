{
  "name": "@exobase/client-js",
  "version": "1.0.0-alpha.24",
  "description": "Javascript client for interacting with the Exobase platform API",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/*"
  ],
  "author": "rayepps",
  "private": false,
  "license": "MIT",
  "scripts": {
    "test": "jest --coverage",
    "check": "yarn lint && yarn test && yarn build",
    "build": "tsc",
    "lint": "tslint -p tsconfig.json",
    "npm:publish": "npm publish --access public",
    "push": "yarn run build && yarn run npm:publish"
  },
  "devDependencies": {
    "@types/chai": "^4.2.21",
    "@types/jest": "^27.0.1",
    "chai": "^4.3.4",
    "jest": "^27.1.0",
    "ts-jest": "^27.0.5",
    "tslint": "^6.0.0",
    "typescript": "^4.4.2"
  },
  "dependencies": {
    "@exobase/client-builder": "^0.0.2-alpha.2"
  }
}
